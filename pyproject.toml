[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "N", "B"]
ignore = ["E701"]  # allow compact one-line guards in threshold logic

[tool.pytest.ini_options]
testpaths = ["tests"]
